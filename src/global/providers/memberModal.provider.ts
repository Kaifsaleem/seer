// import { Provider } from '@nestjs/common';
// import { Connection } from 'mongoose';
// // import { Member, MemberSchema } from '../modules/members/members.schema';

// export const MemberModelProvider: Provider = {
//   provide: 'COMPANY_MODEL',
//   useFactory: async (conn: Connection) => {
//     // return conn.model(Member.name, MemberSchema);
//   },
//   inject: ['COMPANY_CONNECTION'],
// };
// 8;
